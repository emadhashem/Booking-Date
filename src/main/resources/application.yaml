spring:
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;database=bookingDate;encrypt=false
    username: sa
    password: 1234emadA@

  jpa:
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.SQLServerDialect
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create

  main:
    banner-mode: off

logging:
  level:
    root: warn
    org:
      hibernate:
        #        log sql statements
        sql: debug
        orm:
          jdbc:
            #            log the values from statements
            bind: trace
      springframework:
        security: DEBUG
